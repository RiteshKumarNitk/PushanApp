-- Run this line in Supabase SQL Editor
-- If it returns 'true' or 'false', the fix IS applied.
-- If it gives an error "function public.is_admin() does not exist", the fix IS NOT applied.

SELECT public.is_admin();
